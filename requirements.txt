# Core web service
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
python-multipart==0.0.9

# Data / numerics
pandas==3.0.3
numpy==1.26.4          # keep <2 for the trained artifacts
requests==2.34.2

# Model runtime
joblib==1.5.3
scikit-learn==1.9.0
catboost==1.2.10
lightgbm==4.6.0
xgboost==3.2.0

# Persistence / export
sqlalchemy==2.0.50
openpyxl==3.1.5

# Optional: deep-learning (TCN) path. Imported lazily in fx_api_sniper_CLperpair.py.
# Pinned to match the current .venv; comment out if you do not use the TCN model.
torch==2.2.2

# Optional: LLM signal-review path. Imported lazily; falls back to rule-based review
# when the provider package / API key is absent. AI_REVIEW_PROVIDER selects which.
# Default provider is "openai" (gpt-4o-mini); "anthropic" is also supported.
openai==1.54.0
anthropic==0.40.0

# Core web service
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
python-multipart==0.0.9

# Data / numerics
pandas==3.0.3
numpy==1.26.4          # keep <2 for the trained artifacts
requests==2.34.2

# Model runtime
joblib==1.5.3
scikit-learn==1.9.0
catboost==1.2.10
lightgbm==4.6.0
xgboost==3.2.0

# Persistence / export
sqlalchemy==2.0.50
openpyxl==3.1.5

# NOTE: torch (TCN deep-learning path) intentionally excluded — the TCN model is not
# live (tcn_live_allowed=false on every pair) and it bloats the Railway build by ~1GB+.
# Re-add `torch==2.2.2` only if you start training/serving the TCN challenger.

# LLM signal-review path — the second-layer verification run before a trade is placed.
# Imported lazily; AI_REVIEW_PROVIDER selects which (default "openai"/gpt-4o-mini,
# "anthropic" also supported). Requires AI_REVIEW_ENABLED=true and the provider API key.
openai==1.54.0
anthropic==0.40.0
